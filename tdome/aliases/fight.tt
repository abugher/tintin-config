#nop Include this line in every file to set the command character.

#alias {c} {
  #send consider
}
#alias {jump} {
  #send hide;
  #send stun %1;
}
#alias {j} {
  jump %1
}
#alias {f} {
  #send feign;
}
#alias {bs} {
  #send backstab %1;
}
